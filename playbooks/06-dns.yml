---
# file: 06-dns.yml
- name: Playbook that will configure DNS in the homelab. Currently based on pihole.
  hosts:
    - core
  become: true
#  strategy: free # noqa: run-once[play]
  roles:
    - role: waal70.pihole
