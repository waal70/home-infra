# HOWTO work with a Python virtual environment

* Go to the folder you wish to create your environment in
* Run ```python3 -m venv "nameoffolder"```
* Activate by running ```source "nameoffolder"/bin/activate```
* Install by running ```pip```
